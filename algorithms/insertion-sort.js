const insertionSort = (arr) => {
  for (let i = 1; i < arr.length; i++) {
    for (let j = i; j >= 0; j--) {
      // set a tracker?
    }
  }
  return arr;
};

console.log(insertionSort([8, 34, 12, 71, 29, 2, 120, 0]));
